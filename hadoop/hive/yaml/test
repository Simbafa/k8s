cat /tmp/root/hive.log

create database hive;
show databases;
use hive;
create table hive_01 (id int,name string);
./bin/beeline -u "jdbc:hive2://localhost:10000" -n hive -p hive --verbose=true
ldapsearch -x -LLL -H ldap:/// -b dc=justep,dc=com dn
